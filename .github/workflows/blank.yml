name: Class Workflow

on:
  push:
    branches: [ "develop" ]

jobs:

  checkout:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    
    - name: Checkout code
      run: git checkout main
    
  start:
    - name: Run go file
      run: go run main.go
    
